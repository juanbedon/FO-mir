<!DOCTYPE html>
<html>
  <head>
    <title>Flow Overstack</title>
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.1.0/css/all.css" integrity="sha384-lKuwvrZot6UHsBSfcMvOkWwlCMgc0TaWr+30HWe3a4ltaBwTZhyTEggF5tJv8tbt" crossorigin="anonymous">
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container">
        <a class="navbar-brand" href="/">Flow Overstack</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav ml-auto mx-2">
            <% if signed_in? %>
              <li class="nav-item">
                <img src="<%= Gravatar.new("#{current_user.email}").image_url %>" class="rounded" />
              </li>
              <li class="nav-item ml-1 mr-5"><%= link_to "Sign Out", logout_path, method: :delete %></li>
              <li class="nav-item mr-2"><%= link_to "<span class='fas fa-plus'></span>".html_safe, new_question_path %></li>
            <% else %>
              <li class="nav-item mx-2"><%= link_to "Sign Up", new_user_path %></li>
              <li class="nav-item"><%= link_to "Log In", login_path %></li>
            <% end %>
          </ul>
          <form class="form-inline my-2 my-lg-0">
            <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
          </form>
        </div>
      </div>
    </nav>





  	<% if flash[:notice] %>
  		<div class="alert alert-info"><%= flash[:notice] %></div>
  	<% end %>

    <%= yield %>

  </body>
</html>
